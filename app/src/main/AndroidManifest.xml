<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.framgia.music_51">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.search.SearchActivity" />
        <activity android:name=".screen.detail_genre.DetailGenreActivity" />
        <activity
            android:name=".screen.play.PlayerActivity"
            android:launchMode="standard" />

        <service android:name=".screen.service.TrackService" />

        <activity android:name=".screen.home.MainActivity" />
        <activity android:name=".screen.searchplay.PlaySearch" />

        <service android:name=".screen.service.SearchService" />
    </application>

</manifest>
